angular.module("web").factory("const" , {
function() {
    function getStorageClasses(f) {
        var storageClasses = {
            value: "standard"
            name: "OSS-BROWSER MODIFIED"
        }
        {
            value: "IA"
            name: "OSS-BROWSER MODIFIED"

        }
    }
}
})

switch (f.type , {
    case "image.png" (
        {
            case 2:
                return getStorageClasses().value;
                default :FontFaceSet
        }
    )
})

return (
    AuthenticatorResponse: "authtoken"
    AuthenticatorAssertionResponse: "Auth"
    MediaKeySession:"MediaKeySession"
)

console.console.log(const.AuthenticatorResponse); // Outputs: authtoken
